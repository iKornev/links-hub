import React from "react";

const NavBar = () => (
  <nav className="navbar navbar-expand-lg bg-primary">
    <div className="container-fluid">
      <h2 className='text-light'>LinksHub</h2>
    </div>
  </nav>
);

export default NavBar;
